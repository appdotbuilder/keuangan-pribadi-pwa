import { type RecurringRule } from '../schema';

export async function getRecurringRules(userId: string, activeOnly = true): Promise<RecurringRule[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching recurring rules for the user.
    // By default returns only active rules, but can include inactive ones.
    return Promise.resolve([]);
}